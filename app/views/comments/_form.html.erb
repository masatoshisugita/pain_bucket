<div class="box2">
<%= form_with(model: [post, comment] ) do |form| %>
  <div>
    <%= form.text_area :body %>
  </div>
  <div class="actions">
    <%= form.submit "コメントをする" %>
  </div>
<% end %>
</div>
